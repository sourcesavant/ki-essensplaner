# Azure App Registration (required for OneNote import)
# 1. Go to Azure Portal → App registrations → New registration
# 2. Name: "KI-Essensplaner"
# 3. Supported account types: "Personal Microsoft accounts only" (for live.com/outlook.com)
#    OR "Accounts in any organizational directory and personal Microsoft accounts"
# 4. Redirect URI: http://localhost:8400 (Mobile and desktop applications)
# 5. API Permissions: Notes.Read (delegated)
# 6. Copy the Client ID below

AZURE_CLIENT_ID=your-client-id-here
# AZURE_TENANT_ID: Use "consumers" for personal accounts (default), "common" for all, or specific tenant ID
# AZURE_TENANT_ID=consumers

# Optional: OpenAI API key for future phases
OPENAI_API_KEY=your-openai-key-here
